#include <iostream>
using namespace std;

int main() {
    int num, sum = 0;
    cout << "Yek adad 4 raghami vared konid : ";
    cin >> num;
    while (num > 0) {
        sum += num % 10;
        num /= 10;
    }
    cout << "jam argham : " << sum << endl;
    return 0;
}
